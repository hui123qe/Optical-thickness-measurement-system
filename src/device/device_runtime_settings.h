#pragma once

namespace otms::device {

// Set these before the first access to the corresponding device manager.
extern bool g_useVirtualMotionDriver;
extern bool g_useVirtualLaserProbe;

} // namespace otms::device
